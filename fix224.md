# fix224 — Cut release 1.24.0 (bundles fix222 diagnostics) + stop Analyze/Release running concurrently

> **This runbook cuts a complete, ready-to-tag release 1.24.0.** It contains every edit the build machine needs — pubspec bump, changelog entry, regenerated version.json, and the CI concurrency fix — so the freshness check passes and the pipeline doesn't double-fire. Apply ALL edits, then commit everything together, push main, wait for Analyze green, then push tag `v1.24.0`.
>
> **Prerequisite:** fix222 must already be applied (its sql.dart/xtream.dart/settings_view.dart edits are the substance of this release). If fix222 is NOT yet applied, apply it first, then this. (fix220 must NOT be applied — fix222 superseded it.)
>
> **Version decision:** new version is **1.24.0+222** (minor bump to 1.24.0; build `+222` = highest fix number in the release). The two prior failed attempts (1.23.29) are abandoned — do not reuse that number; 1.24.0 is the clean next release.
>
> **Why this failed before:**
>   1. The 1.23.29 release failed the `version.json` freshness check because version.json was never regenerated+committed and no changelog entry existed (notes fell back to generic "Version 1.23.29"). This runbook provides the exact version.json + changelog so that cannot happen.
>   2. Analyze and Release ran simultaneously because a combined `git push main vX.Y.Z` delivers two ref updates (branch → analyze.yml, tag → release.yml) that fire both workflows in parallel. fix218 only gated release.yml internally. This runbook adds a SHARED concurrency group so they queue.

## Part 1 — bump the version

### Fix 224.1 — `pubspec.yaml`
Current code (verbatim — match exactly):
```yaml
version: 1.23.28+218
```
Replacement code (verbatim):
```yaml
version: 1.24.0+222
```

## Part 2 — add the 1.24.0 changelog entry

### Fix 224.2 — `lib/whats_new_modal.dart`
Add the `'1.24.0'` entry at the TOP of the `_changelog` map (immediately after the map opener, before the current first entry `'1.23.28'`).

Current code (verbatim — match exactly):
```dart
  '1.23.28': [
```
Replacement code (verbatim):
```dart
  '1.24.0': [
    'Diagnostics: added refresh timing and raw-source export to help investigate slow source refreshes. No user-facing behavior change.',
  ],
  '1.23.28': [
```
> Note: the changelog string contains no apostrophes (avoids the fix214/216 class of bug). If you edit the text, escape any apostrophe as `\'` or use a double-quoted string.

## Part 3 — regenerated version.json (MUST match what the script produces)

### Fix 224.3 — `version.json`
This is exactly what `python3 scripts/update_version_json.py` produces for 1.24.0 with the 224.2 changelog entry (verified by running the real script). Replace the ENTIRE file contents with:

Current code (verbatim — match exactly):
```json
{
  "latest": "1.23.28",
  "releaseUrl": "https://github.com/rkinnc75/Free4Me-IPTV/releases/tag/v1.23.28",
  "minSupportedAndroidApi": 21,
  "criticalUpdate": false,
  "releaseNotes": "\u2022 CI: Release workflow now gates the APK build on analyze passing. If analyze fails, no build is scheduled."
}
```
Replacement code (verbatim — exact bytes, note `\u2022` for the bullet, trailing newline):
```json
{
  "latest": "1.24.0",
  "releaseUrl": "https://github.com/rkinnc75/Free4Me-IPTV/releases/tag/v1.24.0",
  "minSupportedAndroidApi": 21,
  "criticalUpdate": false,
  "releaseNotes": "\u2022 Diagnostics: added refresh timing and raw-source export to help investigate slow source refreshes. No user-facing behavior change."
}
```
> **Verification:** after applying 224.1–224.3, running `python3 scripts/update_version_json.py` MUST produce NO diff on version.json (the freshness check runs exactly this). If it produces a diff, the changelog text in 224.2 was altered — re-sync them.

## Part 4 — stop Analyze and Release running concurrently

### Fix 224.4 — `.github/workflows/release.yml`: shared concurrency group
Current code (verbatim — match exactly):
```yaml
concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false
```
Replacement code (verbatim):
```yaml
concurrency:
  # fix224: shared group across analyze.yml + release.yml so a combined
  # commit+tag push does not run them simultaneously — they queue instead.
  group: free4me-ci-${{ github.sha }}
  cancel-in-progress: false
```

### Fix 224.5 — `.github/workflows/analyze.yml`: same shared group
Current code (verbatim — match exactly):
```yaml
name: Analyze

on:
  push:
    branches:
      - main

jobs:
```
Replacement code (verbatim):
```yaml
name: Analyze

on:
  push:
    branches:
      - main

concurrency:
  # fix224: shared group with release.yml so the two never run concurrently
  # for the same commit (a combined commit+tag push triggers both).
  group: free4me-ci-${{ github.sha }}
  cancel-in-progress: false

jobs:
```

## Summary table

| Item | Value |
|---|---|
| New version | `1.24.0+222` (tag `v1.24.0`) |
| Files touched | `pubspec.yaml`, `lib/whats_new_modal.dart`, `version.json`, `.github/workflows/release.yml`, `.github/workflows/analyze.yml` |
| Bundles | fix222 diagnostics (must be applied first) |
| App code change | none beyond fix222 + changelog entry |
| New analyzer issues | none expected (2 tolerated INFOs) |

## Release sequence (the build machine MUST do this in order)
1. Confirm fix222 is applied (sql.dart/xtream.dart/settings_view.dart). If not, apply it first.
2. Apply 224.1–224.5.
3. `flutter analyze --no-fatal-infos` → only the 2 tolerated `settings_view.dart` INFOs.
4. `python3 scripts/update_version_json.py` → MUST report no change to version.json (confirms 224.3 matches). If `git diff version.json` is non-empty, STOP and re-sync.
5. Commit ALL changed files together (fix222 files + pubspec.yaml + whats_new_modal.dart + version.json + the two workflow files).
6. Push to `main`. Wait for the **Analyze** workflow to go GREEN.
7. Push tag `v1.24.0`. The **Release** workflow now QUEUES (shared concurrency group) rather than running alongside Analyze, and the version.json check passes.

## Test plan
1. Release workflow completes green; `v1.24.0` published with an APK.
2. In Actions, confirm Analyze and Release for this push did NOT run simultaneously (one queued behind the other).
3. App "What's New" on 1.24.0 shows the diagnostics note.
4. The fix222 diagnostics work: refresh a source, tap Export = log, long-press Export = source dump.

## Notes
- The `+222` build number reflects the highest fix number folded in (fix222). fix224 is CI/release plumbing and doesn't advance the build number further.
- Had fix224's shared concurrency group existed earlier, the parallel Analyze/Release runs would have queued instead — saving the wasted build minutes.
- This supersedes the earlier process-only draft of fix224: it now provides every byte the build machine needs rather than asking a human to do steps.
