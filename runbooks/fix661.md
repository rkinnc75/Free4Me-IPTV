# fix661 — code-review deferred VERIFY-FIRST set (v3.0.6)

**Builder:** desktop. **Live base:** v3.0.5. **Ships as:** v3.0.6+650.

Implements the ~30 device-premise VERIFY-FIRST findings that Waves 1–2 deferred
(they were parked pending on-device confirmation, NOT dropped). 41 findings
applied across 13 files via a disjoint-file fan-out with pinned shared
signatures. analyze clean, 304 tests green.

## Applied (by file)
- **sql.dart**: setAppMeta/getAppMeta (38/43), channelsGen + favoriteChannel bump (77), getChannelsPreserveForBackup (93).
- **mpv_engine.dart**: setHardwareDecode (18), promoteToFullScreen (19), ctor test seam (99), isLive (102).
- **epg_matcher.dart**: buildIndex/matchAgainst split (41).
- **epg_service.dart**: 38/43 (cross-isolate app_meta bridge + in-progress marker), 41 (build index once).
- **main.dart**: 38 (resume/startup poll of epg_last_completed_utc → bump epgVersion).
- **player.dart**: 15 (VOD terminal failure), 17/22/23 (_onCastTap rewrite + stop-local + resume headers), 18/19 (hwdec restore / promote), 20/21 (network-restore + _openGeneration guard), 26/35 (fromMultiView gate), 27/29/31/32/33/34.
- **db_factory.dart**: 54 (buildMigrations seam), 56 (fresh-install marker), 61 (self-heal debounce).
- **tv_guide_view.dart**: 76 (empty-cat focus), 77 (reload on channelsGen).
- **multi_view_cell.dart**: 87/89/90. **overlay_player_widget.dart**: 109/110.
- **xtream.dart**: 62 (off-isolate decode). **settings_io.dart**: 93 (backup-scoped preserve). **settings_service.dart**: 161 (parse try/catch).

## Deferred
- **58** (heavy migration-tail deferral) — larger cross-file refactor (db_factory mig35/38 + sql deferred backfill + main hook); left for a focused pass.

## Verify (ON-DEVICE — this is the point of these findings)
Owner cleared onn testing. After the APK publishes, install on the onn (3.0.x) and:
1. REGRESSION smoke: launch, browse Live/Movies/Series/Categories/History/Search, play a live channel + a VOD, refresh a source, refresh EPG, open the guide — nothing wedges/crashes (the player _onCastTap/_startPlayback/_openGeneration + epg matcher-loop rewrites are the risk).
2. Spot-verify: dead VOD → "can't play" not infinite buffer (15); kill+restore wifi mid-live → auto-resume (20); background→foreground after an EPG refresh → guide updates (38); multi-source EPG refresh completes + matches (41).
See docs/DEVICE_TESTING.md for the per-finding adb procedures.
