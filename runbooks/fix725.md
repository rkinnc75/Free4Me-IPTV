# fix725 — Guide/tiles source-tint + micro-ramp (v4.1.29)

TV only; phone byte-identical. Mock-match (owner: build all approved, don't
stop). Mock: claude.ai artifact e2b0531f (§4.2/§4.3). Closes tasks #42/#43.

**Renumber note:** authored as fix724/v4.1.28 but a PARALLEL commit (the phone
coder) shipped its own fix724/v4.1.28 = "APK Signature Scheme v3" while this was
in flight. Rebased onto that (their build.gradle signing change is preserved),
renumbered to fix725/v4.1.29. Lesson reinforced: another actor pushes to main —
always `git fetch` + check origin before choosing a fix number / version.

## What (unchanged from the fix724 authoring)
- Guide rail cells: source-tinted background (`SourcePalette.tintOver`), not just
  the 5px edge → source-color-coded guide (mock §4.3).
- Micro-ramp: channel name 12→11, programme title 11→10.
- Art-less channel tiles: source-tinted card bg on TV, not flat grey (mock §4.2).

## How
- `tv_guide_view.dart`: `_FocusTile` + `Color? background`; `_channelItem` passes
  the already-computed `tint`; focus via the accent ring; fonts per mock.
- `channel_tile.dart` `_buildPoster`: `posterBg = (showSourceEdgeBar &&
  tintColor != null) ? SourcePalette.tintOver(tintColor, black26) : black26`;
  fallback + both loading placeholders use it. Phone/non-edge unchanged.

## Already present (NOT re-done)
Hero live-preview (fix510), source edge (fix501), genre stripe (fix711), NOW
glow + progress (fix705), gap placeholder (fix706).

## Tests / verify
`test/fix725_source_tint_test.dart` (4). Whole-project analyze clean; suite green.
- onn: multi-source guide → rows tinted per source + accent ring on focus;
  art-less tiles show the source tint. Phone: unchanged.
- Release: commit → tag v4.1.29 → push; poll expanded_assets for -arm.apk.
