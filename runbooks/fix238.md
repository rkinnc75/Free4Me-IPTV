# fix238 — App-wide slow-query logging (Postgres-style "log statements over N ms")

> **Version:** build = highest fix number in release; patch = next available. Tag = next immutable.
> **Builds on:** 1.25.1+236 (apply AFTER fix236). Standalone diagnostic — independent of the refresh fix.
> **What it does:** SQLite has no built-in slow-query log (it's an embedded library, not a server). This adds the app-layer equivalent: a thin wrapper over the database that times EVERY query (reads + writes + queries inside transactions) and logs any statement whose wall-clock duration is ≥ **1000ms**, via `AppLog.warn`. Gated on the SAME debug-logging flag (`AppLog.enabled`) as all other diagnostics — when debug logging is off, the only cost is a Stopwatch per call and nothing is logged.
> **Why 1000ms / app-wide:** 1000ms stays quiet during normal bulk work (channel inserts were <150ms; EPG imports / search-cache rebuilds / a healthy refresh all pass) while still catching anything pathological loud and clear (the broken restore was 134,000ms). App-wide because the wrapper sits at the single `DbFactory.db` chokepoint, so all `Sql.*` queries flow through it with no per-call-site edits.

## Design notes (read before applying)

- The wrapper implements `SqliteWriteContext` — the interface every `DbFactory.db` caller actually uses (`execute`, `getAll`, `get`, `getOptional`, `executeBatch`, `executeMultiple`, `writeTransaction`). It does NOT implement `SqliteDatabase`-only members (e.g. `close()`); the raw instance is retained in `_db` for lifecycle. Verified: nothing in the app calls a `SqliteDatabase`-only method through `DbFactory.db` (the whole app compiles with the narrower return type).
- It intercepts `writeTransaction` so the context handed to the callback is ALSO wrapped — this is what makes queries INSIDE transactions (the refresh commit, etc.) subject to timing. Verified in-sandbox: a slow query inside a `writeTransaction` logs.
- Timing is Dart-side wall-clock, which on this app includes the sqlite_async isolate round-trip — that is exactly the cost that has mattered (the restore slowness was round-trip + plan-choice, both caught here). It will NOT catch a statement that is slow purely inside SQLite but fast to call (rare here).
- `sqlite3` is added as a DIRECT pubspec dependency only so the wrapper can name the `ResultSet`/`Row` return types; it is already bundled transitively (via `sqlite_async` / `sqlite3_flutter_libs`) and resolves to the SAME version (2.9.4) — no version churn.
- Threshold is a top-level `const slowQueryThresholdMs = 1000;` — one-line change to retune (needs a rebuild).
- **Noise caveat:** if debug logging is left on permanently, legitimately-heavy operations that happen to exceed 1s (a very large EPG import, the search-cache rebuild on a huge catalog) will log a SLOW SQL line. That's informational, not harmful. Raise the threshold or remove the wrapper after an investigation if it gets chatty.

## Pre-verified facts (checked against 1.25.1+236, full app compiles)

- New file `lib/backend/timed_db.dart` (the wrapper). Edits to `lib/backend/db_factory.dart` (import + field + getter return type) and `pubspec.yaml` (add `sqlite3`).
- `DbFactory.db` is used ONLY in `sql.dart`, and only via `execute`/`getAll`/`get`/`getOptional`/`writeTransaction` — all on the wrapped interface. The return-type change therefore compiles app-wide.
- **Compile status:** `flutter analyze --no-fatal-infos` → exactly the 2 tolerated `settings_view.dart` INFOs. Rule 6 grep clean. `flutter pub get` re-resolves with `sqlite3` 2.9.4 (unchanged).
- **Behavior verified in-sandbox** with the real package: a sub-threshold query does not log; a >1s query at top level logs `SLOW SQL <ms>: <sql>`; a >1s query inside `writeTransaction` also logs.

## Fix 238.1 — new file `lib/backend/timed_db.dart`

Create this file verbatim:
```dart
import 'package:sqlite3/common.dart' as sqlite;
import 'package:sqlite_async/sqlite_async.dart';

import 'package:open_tv/backend/app_logger.dart';

/// fix238: app-wide slow-query logging. Wraps a [SqliteWriteContext] and times
/// every execute/getAll/get/getOptional call. Any statement whose wall-clock
/// duration (Dart-side — includes the sqlite_async isolate round-trip, which is
/// what actually matters for this app) is at least [slowQueryThresholdMs] is
/// logged via [AppLog.warn], but ONLY when AppLog.enabled (the same debug-log
/// flag that gates all other diagnostics). When disabled the overhead is a
/// single Stopwatch per call and no logging.
///
/// The wrapper also intercepts [writeTransaction] so the [SqliteWriteContext]
/// handed to the callback is itself wrapped — this is what makes queries INSIDE
/// transactions (e.g. the source-refresh commit) subject to the same timing.
///
/// NOTE: this implements [SqliteWriteContext], which is the interface
/// `DbFactory.db` callers actually use (execute/getAll/get/getOptional/
/// writeTransaction). If a future caller needs a `SqliteDatabase`-only member
/// (e.g. close()), call it on the raw instance the factory retains, not through
/// this wrapper. Un-proxied members fall through to [noSuchMethod] on the inner
/// object.
const int slowQueryThresholdMs = 1000;

class TimedWriteContext implements SqliteWriteContext {
  final SqliteWriteContext _inner;

  TimedWriteContext(this._inner);

  Future<T> _timed<T>(String sql, Future<T> Function() run) async {
    if (!AppLog.enabled) return run();
    final sw = Stopwatch()..start();
    try {
      return await run();
    } finally {
      sw.stop();
      if (sw.elapsedMilliseconds >= slowQueryThresholdMs) {
        final flat = sql.trim().replaceAll(RegExp(r'\s+'), ' ');
        final clipped = flat.length > 300 ? '${flat.substring(0, 300)}…' : flat;
        AppLog.warn('SLOW SQL ${sw.elapsedMilliseconds}ms: $clipped');
      }
    }
  }

  @override
  Future<sqlite.ResultSet> execute(String sql,
          [List<Object?> parameters = const []]) =>
      _timed(sql, () => _inner.execute(sql, parameters));

  @override
  Future<sqlite.ResultSet> getAll(String sql,
          [List<Object?> parameters = const []]) =>
      _timed(sql, () => _inner.getAll(sql, parameters));

  @override
  Future<sqlite.Row> get(String sql,
          [List<Object?> parameters = const []]) =>
      _timed(sql, () => _inner.get(sql, parameters));

  @override
  Future<sqlite.Row?> getOptional(String sql,
          [List<Object?> parameters = const []]) =>
      _timed(sql, () => _inner.getOptional(sql, parameters));

  @override
  Future<void> executeBatch(String sql, List<List<Object?>> parameterSets) =>
      _timed(sql, () => _inner.executeBatch(sql, parameterSets));

  @override
  Future<void> executeMultiple(String sql) =>
      _timed(sql, () => _inner.executeMultiple(sql));

  @override
  Future<T> writeTransaction<T>(
          Future<T> Function(SqliteWriteContext tx) callback) =>
      _inner.writeTransaction((tx) => callback(TimedWriteContext(tx)));

  // Any member not explicitly proxied above delegates to the inner context.
  @override
  dynamic noSuchMethod(Invocation invocation) =>
      (_inner as dynamic).noSuchMethod(invocation);
}
```

## Fix 238.2 — `pubspec.yaml`: add `sqlite3` direct dependency

### Current code (verbatim — match exactly)
```yaml
  sqlite_async: ^0.13.0
```
### Replacement code (verbatim)
```yaml
  sqlite_async: ^0.13.0
  sqlite3: ^2.9.0 # fix238: direct dep for ResultSet/Row types used by the slow-query timing wrapper (already bundled transitively via sqlite_async / sqlite3_flutter_libs).
```
> After this, run `flutter pub get`. `sqlite3` should resolve to the version already in pubspec.lock (2.9.4) — no other dependency changes.

## Fix 238.3 — `lib/backend/db_factory.dart`: import the wrapper

### Current code (verbatim — match exactly)
```dart
import 'package:open_tv/backend/app_logger.dart';
import 'package:open_tv/backend/utils.dart';
import 'package:sqlite_async/sqlite_async.dart';
```
### Replacement code (verbatim)
```dart
import 'package:open_tv/backend/app_logger.dart';
import 'package:open_tv/backend/utils.dart';
import 'package:open_tv/backend/timed_db.dart';
import 'package:sqlite_async/sqlite_async.dart';
```

## Fix 238.4 — `lib/backend/db_factory.dart`: add the `_timedDb` field

> The channels `DbFactory` class (the one managing `db.sqlite`) is the FIRST class in the file. Edit its field block only.

### Current code (verbatim — match exactly)
```dart
class DbFactory {
  static SqliteDatabase? _db;

  static Future<SqliteDatabase> _createDB() async {
    var db = SqliteDatabase(path: "${await Utils.appDir}/db.sqlite");
```
### Replacement code (verbatim)
```dart
class DbFactory {
  static SqliteDatabase? _db;
  // fix238: cached timing wrapper handed out by the `db` getter.
  static TimedWriteContext? _timedDb;

  static Future<SqliteDatabase> _createDB() async {
    var db = SqliteDatabase(path: "${await Utils.appDir}/db.sqlite");
```

## Fix 238.5 — `lib/backend/db_factory.dart`: return the wrapper from the channels `db` getter

> There are TWO `get db` getters in this file (channels DB and EPG DB). Edit ONLY the channels one — uniquely identified by the `/// Manages the EPG-specific SQLite database` comment that immediately follows it. Do NOT change the EPG getter.

### Current code (verbatim — match exactly)
```dart
  static Future<SqliteDatabase> get db async {
    _db ??= await _createDB();
    return _db!;
  }
}

/// Manages the EPG-specific SQLite database (`epg.sqlite`).
```
### Replacement code (verbatim)
```dart
  static Future<SqliteWriteContext> get db async {
    _db ??= await _createDB();
    // fix238: hand callers a timing wrapper (slow-query logging) instead of the
    // raw database. The wrapper exposes the SqliteWriteContext surface all
    // callers use; the raw instance is retained in _db for lifecycle.
    _timedDb ??= TimedWriteContext(_db!);
    return _timedDb!;
  }
}

/// Manages the EPG-specific SQLite database (`epg.sqlite`).
```

## Summary table

| Item | Value |
|---|---|
| Files touched | NEW `lib/backend/timed_db.dart`; `lib/backend/db_factory.dart` (import + field + getter); `pubspec.yaml` (add `sqlite3`) |
| Coverage | all `Sql.*` queries — reads, writes, and queries inside transactions |
| Threshold | `const slowQueryThresholdMs = 1000` (one-line retune) |
| Gating | same `AppLog.enabled` debug flag; no cost / no logging when off |
| New dependency | `sqlite3: ^2.9.0` (resolves to existing 2.9.4 — no churn) |
| New analyzer issues | none (2 pre-existing tolerated INFOs) |

## Pre-tag gate
1. Apply 238.1–238.5.
2. `flutter pub get` → confirm `sqlite3` resolves to 2.9.4 (no other lock changes).
3. `flutter analyze --no-fatal-infos` → only the 2 tolerated INFOs.
4. Bump pubspec build/patch; commit; push main; wait for Analyze green; tag; push tag.

## Test plan
1. Turn debug logging ON. Use the app normally — the log should be quiet (no SLOW SQL lines) during healthy operation, including a normal source refresh (post-fix236 the restore is fast).
2. To confirm it fires: any genuinely slow operation logs `SLOW SQL <ms>: <statement>`.
3. Turn debug logging OFF → no SLOW SQL lines regardless of query duration.

## Notes
- This is a DIAGNOSTIC. Once you're done hunting slow queries, it can be removed (delete timed_db.dart, revert the db_factory getter to return `SqliteDatabase`/`_db`, drop the `sqlite3` dep) — or left in place gated behind the debug flag.
- Complements the fix222 diagnostics. When you do a cleanup pass, fix222's per-closure timing + raw-dump export + EXPLAIN logger and this wrapper can be removed together.
- The logged SQL is whitespace-flattened and clipped to 300 chars to keep log lines readable; parameters are not logged (avoids leaking values into the log).
