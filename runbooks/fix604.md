# fix604 — v2.2.15: EPG timeline boundary-snap + 12/24-hour clock

The two layout/format items deferred from fix603 (v2.2.14), from the user's
v2.2.13 test.

## #4 — timeline labels snap to :00/:30 boundaries
Before: `_timeHeader` drew 4 evenly-spaced ticks at `now`, `now+1h`, `now+2h`,
`now+3h` → "21:38, 22:38, 23:38" (odd minutes, and the labels didn't sit above
the programmes they marked).

After: the header is a `Stack` (LayoutBuilder for width) that
- pins the CURRENT time at the left edge (white70, bold), and
- places labels at every clean :00/:30 boundary in the window, each POSITIONED
  via the same `_x(epoch, width)` mapping the grid blocks use — so a label sits
  above its programmes. A boundary within 44px of the left "now" label is
  skipped to avoid overlap. Horizontal padding (4) matches `_gridRow` so header
  and grid share the same left origin.

(30-minute granularity. If hourly is preferred it's a one-line `step` change.)

## #5 — 12/24-hour clock option (default 12-hour)
New `Settings.use24HourTime` (default **false** = 12-hour). Wired like
`dvrEnabled`:
- `lib/models/settings.dart`: field + ctor default false.
- `lib/backend/settings_service.dart`: `use24HourTimeProp`, read (`== 'true'`),
  write (`.toString()`).
- `lib/settings_view.dart`: a `_switchTile` "24-hour clock in guide" (Playback
  section, by "Live preview in TV guide"); also added to the
  `preserveLibraryPreferences` clone so a settings-reset keeps it.
- `lib/tv/tv_guide_view.dart`: `_clockFmt` getter → `DateFormat.jm()` (12h) or
  `DateFormat.Hm()` (24h); `_fmtEpoch()` helper used by the timeline header and
  the hero NOW/NEXT lines. Removed the old top-level `_timeFmt`.

Default 12-hour means existing users (no stored prop → ctor default) see e.g.
"9:38 PM".

## Adversarial-review follow-ups (same release)
- **MEDIUM**: the setting only reached the Live guide; three other EPG time
  displays were still hardcoded 24-hour. Added a shared `guideClockFmt()` in
  settings_service and routed ALL of them through it: the Live guide, the
  now/next strip (`now_next_strip.dart` — channel tiles + multi-view), the
  per-channel schedule (`channel_schedule.dart`), and the player now-label
  (`player_epg_now_label.dart`). Now consistent app-wide.
- **LOW**: the rightmost boundary label could clip at the grid's right edge —
  added a symmetric `x > width - 44` skip (mirrors the left `x < 44` skip).
- **NIT (left as-is)**: the left "now" label is the window-START time (fixed
  edge) while the grid now-line is live `DateTime.now()`; they diverge as time
  passes until the next reload. This is correct window-edge semantics (the
  window doesn't slide), and re-anchors on every `_loadGuide`.

## Build / release
`flutter analyze` (4 files) clean. Adversarial review (review-fix604) gates the
commit. Single-tag push. Ships on top of v2.2.14; both verified together
on-device (v2.2.15 build includes the v2.2.14 behavioral fixes).
