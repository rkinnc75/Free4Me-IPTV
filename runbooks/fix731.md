# fix731 — Player OSD animated fade + token scrim (mock §4.6/§5) — v4.1.34

TV only. Gap-audit item #2 (high payoff). The control OSD used to snap
mount↔unmount (`if (_navMode) _buildTvOverlay()`); now it fades in/out and uses
the token scrim instead of a static black54 gradient.

## How
- `player.dart` `_buildTvOverlay`: the overlay is ALWAYS mounted and wrapped
  `IgnorePointer(!_navMode) → ExcludeFocus(!_navMode) → AnimatedOpacity(_navMode
  ? 1:0, crossIn/crossOut, easeOut) → FocusTraversalGroup → Container`. The
  mount site became a bare `_buildTvOverlay()`.
- Scrim: `tokens.colors.panelSlate.withValues(alpha: tokens.scrim.playerMenu)`
  (0.6) → transparent middle, replacing `[black54, transparent, black54]`.
- The D-pad model is unchanged: `ExcludeFocus(excluding:true)` makes the whole
  overlay subtree (incl. `_overlayFirstFocus`) unreachable by traversal AND
  explicit requestFocus while `!_navMode`; `Opacity(0)` skips paint (no
  per-frame cost).

## Adversarial review (rr)
Verdict SHIP — focus/lifecycle/perf all sound (verified vs the pinned SDK). One
follow-up applied: the always-mounted `PlayerEpgNowLabel` would have polled
`Sql.getNowNext` every 30s for the whole session even when hidden → threaded
`active: _navMode` through `PlayerInfoBar` so the poll pauses while the OSD is
hidden (onn DB-lock sensitivity).

## Verify
`test/fix731_osd_fade_test.dart` (4). Whole-project analyze clean.
- onn: open a channel → OK reveals the OSD with a fade-in; wait 8s → fades out;
  D-pad still surfs/seeks after (no dead D-pad). Watch a live channel with OSD
  closed → EPG poll paused.
- Release: commit → tag v4.1.34 → push.
