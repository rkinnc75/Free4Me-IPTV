# fix655 — code-review resolution, partial ship (v3.0.0)

**Builder:** desktop. **Live base:** v2.2.54. **Ships as:** v3.0.0+644.

First release of the independent code-review resolution (`docs/START_HERE.md` →
`docs/IMPLEMENTATION_SPEC.md`). Major-version bump per the owner's decision to
"start at 3.0.0". This ships **Wave 1 + the first 3 files of Wave 2**; the rest
of Wave 2 continues under `docs/REVIEW_PROGRESS.md` and will ship as 3.0.x.

## Resolved findings (all local commits, analyze clean, 292 tests green)

**Wave 1 (security/settings core)**
- `app_logger.dart` — 45/40/82: credential redaction at source (URL-encoded +
  query-string creds + EPG path tokens); clears log-side leaks in xmltv/epg/m3u.
- `settings_view.dart` — **P0-1** (export credential leak gated), 2/3/4/7
  (EPG/TV-export/issue-report dialog Back-abort), 5/105/103/104 (reset/optimise
  preserve safeMode + searchMethod, RAM-aware reset), 6/8/10/11/12/14.
- `settings.dart` + `settings_service.dart` — 106, 160, 161 (ViewType boot-brick
  hardening), 162.
- `settings_io.dart` — 92 (P1 backup credential scrub), 94/95/96/97.

**Wave 2 (files 5–7)**
- `m3u.dart` — 78 (deferred+gated catalog wipe; no total loss on empty/HTML/
  truncated download), 79 (bulk insert), 80 (temp cleanup+sweep), 81 (stall→
  error), 83/84.
- `xmltv_parser.dart` — 46 (truncation result), 47/48 (backpressure/cancel), 49
  (host-only errors), 50 (allowMalformed), 51 (window overlap), 52/53.
- `epg_service.dart` — 46 (caller), 36 (FTS rebuild hoisted once-after-all), 37
  (successful-only refresh debounce, sql.dart), 39 (per-source try/catch), 42
  (isStale on-now), 44 (dead-code delete).

Tests added: `app_logger_redaction_test`, `m3u_getname_test`, a use24HourTime
round-trip case, `settings_io`/`settings.dart` covered via existing suites.

## Deferred (NOT in this release — tracked in docs/REVIEW_PROGRESS.md)
- epg 38+43 (cross-isolate app_meta bridge + main.dart resume poll), 41 (VF
  matcher index-reuse refactor).
- settings_view 9/13 (P3 verify-first, cross-file), settings_io 93 (P2
  verify-first perf), settings_service 161-step2 (broad try/catch).
- All of Wave 2 files 8–22 (player.dart onward).

## Verify
1. `flutter analyze` clean; `flutter test` green (292).
2. **On-device (pending):** the MANUAL/DEVICE findings have NOT yet been
   exercised on hardware. After the CI APK publishes, install on the onn and
   smoke-test: launch, refresh a source (M3U + Xtream), refresh EPG, confirm no
   catalog loss / no crash. Full device-verify matrix per `docs/DEVICE_TESTING.md`
   is still batched for the end of Wave 2.
