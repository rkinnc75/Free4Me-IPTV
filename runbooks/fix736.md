# fix736 — Guide → Program schedule & record (mock §4.3) — v4.1.39

TV Live guide. Answers the owner's Q: "can a user move to a future show, read
details, and record it via the Scheduled Recording arc?" — YES, now from the guide.

## What (Phase A — safe capability delivery; in-grid nav = Phase B, separate)
The guide grid cells are deliberately passive (ExcludeFocus, finding 75). Rather
than risk focus-model surgery on the 1405-line flagship, the guide's held-OK
channel menu gains **"Program guide & record"** (live only) → pushes the existing
`ChannelScheduleView(channel: ch)`: a scrollable list of the channel's programmes
(past/now/FUTURE), each with a details dialog + a **Record** action for
future/on-now programmes → `RecordingActions.recordProgramme` →
`RecordingScheduler.scheduleForProgramme` (the real alarm-driven SR arc, fix667/678).
Reuses proven code; zero new recording plumbing.

## Verify
`test/fix736_guide_schedule_test.dart` (2). Whole-project analyze clean.
- onn: Live guide → held-OK on a channel → "Program guide & record" → the
  schedule list opens; pick a future programme → details → Record → "Recording
  scheduled"; appears in Recordings as scheduled.
- Release: commit → tag v4.1.39 → push.
