# fix703 — TV GUI redesign Phase 2: channel/poster tile focus (v4.1.8)

**Builder:** desktop. **Live base:** v4.1.7. **Ships as:** v4.1.8+703. TV only;
phone UI byte-identical. Standing autonomous GUI mandate.

## What
`lib/channel_tile.dart` — the focused TV tile now uses the token focus look,
consistent with the tab bar:
- **Ring:** `Colors.yellow` (3px) → `AccentScope.of(context)` (2.5px), gated on
  the UNCHANGED `showSourceEdgeBar && _focusNode.hasFocus`. `showSourceEdgeBar`
  is the TV signal (true in tv_browse/tv_categories/tv_search; `!hasTouchScreen`
  in home.dart) → TV-only. AccentScope sits INSIDE the gated ternary, so on the
  phone build the gate is false and `AccentScope.of` is never evaluated → no
  dependency registered → phone provably untouched.
- **Lift:** `return Card(...)` → `final Widget tile = Card(...)`; on the TV path
  the tile is wrapped in `AnimatedScale(1.05× on focus, F4Motion.normal/easeOut,
  RepaintBoundary)`. Phone path `if (!showSourceEdgeBar) return tile;` returns
  the bare Card (identical to before).

**Deliberately NOT delegated to `TvFocusable`.** ChannelTile owns dense
specialized key handling (LEFT-edge-back fix643, RIGHT→checkbox focusInDirection,
UP→search up-escape fix563, held-OK fix586, prewarm, ensureVisible) bound to its
own (possibly caller-supplied, fix558) FocusNode. A full TvFocusable swap would
risk regressions across Live/Movies/Series/Categories/Search + the phone. So
Phase 2 adopts the token VISUAL (accent ring + lift) and keeps the proven focus
machinery. Held-OK stays fire-at-threshold here (the §3.4 fire-on-release
unification is a separate future refinement — not a regression).

## Review (adversarial, general-purpose agent)
Verdict: **no functional defects — ship it.** Confirmed: phone byte-identical
(AccentScope not read on phone build); focus/onKeyEvent/ensureVisible/checkbox-
nav/held-OK/prewarm intact (scale+RepaintBoundary are paint-only, not focus
boundaries; hit-tests transform); no neighbor overlap (1.05× grows ≈3px/side <
the 6px grid gap; layout unaffected — Transform is paint-only). Cosmetic notes:
(1) LOW — top grid row's ring can be shaved ~3px by the viewport clip when it
can't center-scroll (fix = a few px grid top-inset if device shows it);
(2) negligible resample softening. Watch #1 on device.

## Tests
`test/fix703_tile_focus_test.dart` (4): accent-ring (no yellow), TV lift + phone
bare-tile return, specialized focus machinery kept (no TvFocusable). Full suite
387 green, analyze clean.

## Verify (onn, D-pad) + Release
- onn Movies/Series/Live grids: focused poster shows the accent (white) ring +
  subtle lift; no neighbor overlap; D-pad nav + edge-back + (touch) context menu
  intact. Watch the TOP row's ring for the ~3px clip. Phone: tiles unchanged.
- Release: commit fix703 → tag v4.1.8 → push HEAD + tag (rebase if CI sync raced).
