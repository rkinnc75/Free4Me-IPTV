# fix741 — Guide rail channel logos + initials fallback (mock §4.3) — v4.1.44
TV Live guide rail. Each channel cell now shows a 26px logo (CachedNetworkImage,
contained) before the name, with a typographic-initials fallback
(`_channelInitials`, source-tinted box) so a logo-less channel is never a blank
box (Peer4 branded fallback). Fits the 56px itemExtent; edge bar / name / star /
focus ring / place-memory untouched. `test/fix741_rail_logo_test.dart` (2).
Full `flutter test` +531 green; analyze clean.
