# fix738 — Guide EPG display polish (mock §4.3) — v4.1.41
TV guide, display-only (no focus change — independent of the in-grid-nav follow-up).
- Past-programme DIM: `_block` wraps the cell in `Opacity(pastProgramDim=0.55)`
  when `p.stopUtc <= nowEpoch` (Peer4 dimmed past cells).
- LIVE badge: on the on-now cell, a small white-on-`liveRed` chip (top-right).
  NEW/ARCHIVE omitted — Program has no is-new/catch-up flag (cf. fix705).
- NOW pill: `_timeHeader` left label → "NOW hh:mm" on an accent-tinted pill
  (recolors with the accent picker).
`test/fix738_guide_display_test.dart` (3). Whole-project analyze clean.
Device-verify on the onn guide (needs EPG data): past shows faded, on-now cell
has LIVE + genre stripe + progress, header shows the NOW pill.
