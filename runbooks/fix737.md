# fix737 — Recordings rows → token glass cards (mock §4.9) — v4.1.40
TV only (phone early-returns the bare tile). The TV recording row's
AnimatedContainer (fix718 accent ring) now also carries `glassFill` + a small
inter-card gap + a `glassStroke` unfocused edge, so Recordings reads as glass
cards like the rest of the redesign (was a bare Material ListTile on a flat
Scaffold). Focus ring (fix718) + focus node + held-OK untouched.
`test/fix737_recordings_glass_test.dart` (2). Analyze clean.
