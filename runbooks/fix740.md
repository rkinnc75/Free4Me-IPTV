# fix740 — Poster branded fallback (mock §4.2) — v4.1.43
TV/all: art-less poster tiles now show a BRANDED gradient fill (source tint
`posterTop` → near-black `posterBg`) + the channel name centered over the art
region, instead of a flat fill + a lone generic movie glyph ("never a blank box").
`channel_tile.dart` `_buildPoster` fallback DecoratedBox(gradient) + centered
Text(name). Loading placeholders + source-tint (fix725) unchanged. Updated
fix725's source-check test to the gradient fallback. Full `flutter test` +529 green.
`test/fix740_poster_fallback_test.dart` (2). Whole-project analyze clean.
