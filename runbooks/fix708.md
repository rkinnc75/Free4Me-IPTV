# fix708 — TV GUI redesign Phase 3 unit 3: guide genre tint (v4.1.13)

**Builder:** desktop. **Live base:** v4.1.12. **Ships as:** v4.1.13+708. TV only;
phone UI byte-identical. Standing autonomous GUI mandate.

## What
The on-now guide cell gets a **3px left-edge genre colour stripe**.

- New `lib/tv/theme/genre.dart`:
  - `normalizeGenre(String? category)` → one of the 7 bucket keys of
    `kGenreColors` (news/sport/movies/kids/music/docs/general). Case-insensitive
    substring, most-specific-first (news→sport→kids→music→docs→movies), `general`
    fallback for null/unknown. Never guesses a vivid colour it isn't sure of.
  - `genreEdgeColor(String? category)` → the vivid (`.$1`) colour of the bucket.
- `tv_guide_view.dart` `_block`: for the on-now cell only (`if (isNow)`), a
  `Positioned(left:0, top:0, bottom:0, width:3)` `Container(color:
  genreEdgeColor(p.category))` added to the `Stack`, under the title and over the
  fix705 progress fill. Clipped to the cell radius by the existing fix705
  `clipBehavior: isNow ? Clip.antiAlias : Clip.none`.

## Why per on-now cell (not per channel)
There is no per-channel genre in the data model (locked product decision — the
browse rail shows the provider's categories, not curated genres). The only genre
signal is `Program.category` (free-text XMLTV). A channel airs many genres through
the day, so the tint is per **on-now programme**. No `EnrichedChannel` layer
needed — normalize at render. (`p.category` is selected in `Sql` `_rowToProgram`,
so the tint is data-driven.)

## Review (adversarial, general-purpose agent) — SHIP
Confirmed: never throws; every returned bucket is a real `kGenreColors` key (no
broken-key path); `general` fallback total; ordering correct (children's film→
kids, documentary film→docs, sports news→news); stripe clipped + under title
(title padding starts x=6, stripe x=0–3) + over fill; positioned child → no
`Stack` size / `_rowHeight=56` change; per-on-now-cell scope correct; perf
trivial (~1 lookup/visible row). **Fix applied from review:** `'mma'` (bare 3-char
→ false-hit "programma"/"grammar") → `'mixed martial'`. (`family`/`animat`→kids
left as a defensible coarse-bucket call.)

## Tests
`test/fix708_genre_test.dart` (8): per-bucket case-insensitive match; precedence;
false-positive guard (programma/grammar → general, "mixed martial"→sport); total
fallback; every bucket a valid key; edge-colour = vivid member; guide wiring
(import + 3px on-now stripe). Full suite 416 green, analyze clean.

## Verify (onn) + Release — NOTE
The onn EPG is fresh (loaded ~11h ago) but the loop-feed channels I browsed
(ABC affiliates / Disney+ / Hulu) are **unmatched** (no XMLTV `tvg-id`) → no
cells, so the tint needs a **matched** channel (mainstream national feed mapped
to the epg-us.xml / provider XMLTV) to be visible. A "Refresh EPG now" is a no-op
here (the app confirms the guide is recent). Verify on a channel that shows real
programme cells: the on-now cell has a coloured left stripe matching its genre.
Phone: guide unchanged.
- Release: commit fix708 → tag v4.1.13 → push HEAD + tag (rebase if CI sync raced).
