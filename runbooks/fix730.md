# fix730 — Settings rail selection follows the accent (mock §4.8) — v4.1.33

TV only. First item from the mock-vs-code gap audit (2026-07-12): high payoff,
trivial. The accent focus-ring language (fix704-722) recolors every focus ring
to the chosen accent, but the Settings two-pane rail SELECTION highlight stayed
seed-blue (colorScheme.primary) — visibly inconsistent when a non-white accent
is picked.

## Fix
`settings_view.dart` `_buildTvRailPane` itemBuilder: the selected row's
background tint and icon now read `AccentScope.of(context)` instead of
`Theme.of(context).colorScheme.primary`. Rebuilds reactively via the fix719
MaterialApp `ValueListenableBuilder(appAccentNotifier)` when the accent changes.

## Verify
`test/fix730_settings_accent_test.dart` (4). Whole-project analyze clean.
- onn: Settings ▸ Playback ▸ Accent color → Magenta; the selected rail row bg +
  icon turn magenta (was blue). Default (white) → subtle white highlight.
- Release: commit → tag v4.1.33 → push; poll expanded_assets for -arm.apk.
