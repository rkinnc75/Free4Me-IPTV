# fix734 — TV search field glass + accent ring (mock §4.5) — v4.1.37

TV only (tv_search_view is TV-only). Gap-audit medium item. The Search field was
a borderless default-filled `InputDecoration`; now it reads the token glass fill
+ an accent focus ring, matching the rest of the accent language.

## Fix
`tv_search_view.dart` InputDecoration: `fillColor: F4.of(context).colors.glassFill`,
enabled/default border = `glassStroke`, `focusedBorder` = `AccentScope.of(context)`
width 2 (recolors live with the chosen accent, fix719). No hasTouchScreen gate
(the view never mounts on phone).

## Verify
`test/fix734_search_glass_test.dart` (3). Whole-project analyze clean.
- onn: Search tab → the field has a subtle glass fill; focusing it shows the
  accent ring (white default, follows the accent picker).
- Release: commit → tag v4.1.37 → push.
