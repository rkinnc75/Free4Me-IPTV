# fix582 — re-enable force-30fps cap (#2) + Performance/low-RAM settings group (#3)

**Version:** v2.1.9+582 (also carries the unversioned fix581: #19 empty-state, #21 doc prune)
**Date:** 2026-06-27
**Type:** Playback (low-RAM) + settings UX

## #2 — Force-30fps cap re-enabled (was parked)

fix570 hard-disabled the 60→30 fps `vf` output cap (`const capFps=false`,
`vf=''`) because the BUNDLED libmpv had no `fps` libavfilter filter (a missing
filter makes mpv deselect the video track → black-screen stall). The custom
LGPL-max libmpv now shipped (vnext, via `dependency_overrides`) includes `fps`,
and the cap was verified on-device (vfFps=30, voDrop=0) on the
`libmpv-lgplmax-verify` branch.

`mpv_engine.dart` `_applyMpvOptions` now does:
```dart
final capFps = !previewMode && s.devCapFpsLowRam && await DeviceDetector.isLowRamDevice();
await np.setProperty('vf', capFps ? r'lavfi=[fps=fps=30]' : '');
```
- Only the **vf-cap** was taken from the branch; main's fix571 `framedrop=decoder`
  low-RAM auto-upgrade is left intact (the branch was a pre-fix571 fork).
- **Default flipped OFF** (`devCapFpsLowRam` constructor + reset defaults
  `true→false`): opt-in, because `framedrop=decoder` already holds full-rate
  0-drop on low-RAM, so the 30 fps cap (which halves the frame rate) is only for
  boxes that still judder. Help text updated (was "Default: ON" + inert).
- Form `lavfi=[fps=fps=30]` (no commas to escape); `isVfOptionError` suppression
  (fix566) stays as defensive cover.

## #3 — Performance / low-RAM grouping

The two low-RAM toggles were in separate sections (render cap in Playback,
force-30 in Developer). Moved the **Cap 60→30 fps** tile out of Developer to sit
directly after **Render at 1080p on 4K** in Playback, co-locating both low-RAM
performance toggles (settings_view uses no sub-headers within sections, so a
mid-list header would orphan following tiles — adjacency is the clean fit).

## Verification

- `flutter analyze` clean; `flutter test` green (272).
- **On-device (post-ship):** default OFF → a 60 fps stream (e.g. SP-YES 1080p60)
  stays ~60 fps (STATS `vfFps≈60`); toggle ON in Settings → Playback → tune a
  60 fps stream → `vfFps≈30`, `srcFps≈60`, `voDrop≈0`, no stall/black screen.
