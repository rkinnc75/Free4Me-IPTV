# fix606 — v2.2.17: drop AM/PM from the 12-hour guide clock

User: "AM/PM are not needed in the display for the EPG, it's assumed."

`guideClockFmt()` (settings_service.dart) 12-hour branch changed from
`DateFormat.jm()` ("9:38 PM") to `DateFormat('h:mm')` ("9:38"). 24-hour branch
unchanged (`DateFormat.Hm()` → "21:38"). Since every guide/EPG time routes
through `guideClockFmt()` (fix604), this applies everywhere at once: Live-guide
timeline + hero NOW/NEXT, now/next strip, per-channel schedule, player now-label.

Trivial format-string change — solo'd (analyze + on-device screenshot), no
review workflow.
