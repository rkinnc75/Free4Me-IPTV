# fix334 — ExoPlayer black screen persists until cast pressed (first-frame stall) → v1.26.50+334

> ## WARNING BUILD MACHINE — APPLY THE PATCH, then RELEASE (EXACTLY). No questions, no DB to query.
> **FIRST:** `find . -name 'fix296-DIAGNOSE*.md' -delete 2>/dev/null || true`
> **ORDER:** built on live main 1.26.48+332. fix333 (1.26.49) is also queued; both are independent files (333=channel_tile, 334=exo_engine) but the pubspec/changelog hunks assume 333 lands first. If applying 334 BEFORE 333, the version/changelog hunks rebase trivially (Rich reassigns). No code conflict either way.

## Why (onn 4K Plus, "300" on fix332/1.26.48)
fix332 was not enough. The "300" session logged `ExoEngine: SURFACE[mid-playback+2s] initialized=true size=1920x800 playing=true buffering=false` — the controller was fully healthy — yet the screen stayed black until the user pressed the **cast** button, which made the picture appear. That is the Android `video_player` first-frame stall: the platform texture is created but its first frame is not pushed to the surface until a layout pass occurs. The cast button mounted UI → forced a relayout → frame appeared. fix332 made the widget MOUNT reactively but did not force the post-attach relayout.

## What this does
- Wraps the ExoPlayer `VideoPlayer` in a one-shot `_FirstFrameNudge` that toggles a sub-pixel bottom padding a few times over the first ~600ms after mount, forcing the relayout that pushes the first frame to the surface — reproducing the cast-button effect automatically. Imperceptible; stops after ~5 ticks; cancels on dispose.
- libmpv path untouched.

## Confidence / honesty
- This is the standard remedy for the `video_player` Android first-frame-stall and is low-risk, but unlike the DB/migration fixes it CANNOT be proven in-container (no Android render surface here). The `SURFACE` log already confirms the diagnosis (healthy controller, no paint), and the cast-button workaround confirms a relayout fixes it — this automates that relayout. Treat the on-device check as the real verification.

## Verified before delivery
- `flutter analyze --no-fatal-infos` → the 2 tolerated `settings_view.dart` INFOs only.
- `flutter test` → 18/18 pass.
- Built on live main (1.26.48+332); `git apply --check` CLEAN on a fresh clone.
- Rule 8 N/A (no SQL/migration).

---

## A. Apply this patch (preferred) — `git apply fix334.patch`
```diff
diff --git a/lib/player/exo_engine.dart b/lib/player/exo_engine.dart
index 49006d1..7ed4c34 100644
--- a/lib/player/exo_engine.dart
+++ b/lib/player/exo_engine.dart
@@ -45,9 +45,19 @@ class ExoEngine implements PlayerEngine {
         if (!value.isInitialized) {
           return const ColoredBox(color: Colors.black);
         }
-        return AspectRatio(
-          aspectRatio: value.aspectRatio,
-          child: VideoPlayer(ctrl),
+        // fix334: on the onn 4K Plus the controller is fully healthy
+        // (initialized, real size, playing) yet the screen stays black until
+        // something forces a relayout — the user found that pressing the cast
+        // button made the picture appear. That is the Android video_player
+        // first-frame stall: the platform texture is created but the first
+        // frame is not pushed to the surface until a layout pass occurs.
+        // _FirstFrameNudge forces exactly one post-frame relayout after mount,
+        // reproducing the cast-button effect automatically.
+        return _FirstFrameNudge(
+          child: AspectRatio(
+            aspectRatio: value.aspectRatio,
+            child: VideoPlayer(ctrl),
+          ),
         );
       },
     );
@@ -196,3 +206,56 @@ class ExoEngine implements PlayerEngine {
     _pollTimer = null;
   }
 }
+
+/// fix334: forces a single post-frame relayout after first mount so the
+/// Android video_player platform texture pushes its first frame to the
+/// surface. Without this, some boxes (onn 4K Plus) show audio-only black until
+/// an unrelated relayout (e.g. opening the cast menu) happens to trigger it.
+/// The nudge is a one-shot sub-pixel padding toggle on the next two frames;
+/// it is imperceptible and runs once per mount.
+class _FirstFrameNudge extends StatefulWidget {
+  const _FirstFrameNudge({required this.child});
+  final Widget child;
+
+  @override
+  State<_FirstFrameNudge> createState() => _FirstFrameNudgeState();
+}
+
+class _FirstFrameNudgeState extends State<_FirstFrameNudge> {
+  double _pad = 0.001;
+  Timer? _timer;
+  int _ticks = 0;
+
+  @override
+  void initState() {
+    super.initState();
+    // The texture attaches somewhere in the first few hundred ms after mount.
+    // Toggle a sub-pixel pad a handful of times across that window to force a
+    // relayout once the surface is ready, then stop. Imperceptible; one-shot.
+    _timer = Timer.periodic(const Duration(milliseconds: 120), (t) {
+      if (!mounted) {
+        t.cancel();
+        return;
+      }
+      setState(() => _pad = _pad == 0.0 ? 0.001 : 0.0);
+      if (++_ticks >= 5) {
+        t.cancel();
+        if (mounted) setState(() => _pad = 0.0);
+      }
+    });
+  }
+
+  @override
+  void dispose() {
+    _timer?.cancel();
+    super.dispose();
+  }
+
+  @override
+  Widget build(BuildContext context) {
+    return Padding(
+      padding: EdgeInsets.only(bottom: _pad),
+      child: widget.child,
+    );
+  }
+}
diff --git a/lib/whats_new_modal.dart b/lib/whats_new_modal.dart
index 5e7e28e..d9ac501 100644
--- a/lib/whats_new_modal.dart
+++ b/lib/whats_new_modal.dart
@@ -6,6 +6,9 @@ import 'package:open_tv/backend/settings_service.dart';
 /// in that minor". The dialog shows all entries for [version] whose key is
 /// a prefix of the running version string.
 const _changelog = <String, List<String>>{
+  '1.26.50': [
+    'Further fix for ExoPlayer black-screen-with-audio on some TV boxes: the video now paints its first frame automatically instead of staying black until an unrelated action (like opening the cast menu) forced it to appear.',
+  ],
   '1.26.48': [
     'Fixed a black screen with working audio when playing some movies/streams on the ExoPlayer engine: the video now appears as soon as it is ready instead of staying black.',
   ],
diff --git a/pubspec.yaml b/pubspec.yaml
index ac53a10..b401326 100644
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -16,7 +16,7 @@ publish_to: "none" # Remove this line if you wish to publish to pub.dev
 # https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
 # In Windows, build-name is used as the major, minor, and patch parts
 # of the product and file versions while build-number is used as the build suffix.
-version: 1.26.48+332
+version: 1.26.50+334
 
 environment:
   sdk: ^3.10.7
```

## B. Files changed
1. `lib/player/exo_engine.dart` — `_FirstFrameNudge` wrapper forcing post-mount relayout.
2. `lib/whats_new_modal.dart` — 1.26.50 changelog.
3. `pubspec.yaml` — 1.26.50+334.

## Verification before tagging
1. `flutter analyze --no-fatal-infos` → exactly **2 issues**. Else STOP.
2. `flutter test` → **All tests passed!** (18). Else STOP.
3. `grep '^version:' pubspec.yaml` → `version: 1.26.50+334`.
4. `grep -c "  '1.26.50':" lib/whats_new_modal.dart` → `1`.
5. `grep -c '_FirstFrameNudge' lib/player/exo_engine.dart` → `3` (class def + state + use).
6. No DB migration.
7. **Exercise on TV (onn 4K Plus, the real test):** with engine ExoPlayer/exoLibmpv, play "300" (or any Dino .mp4) that was black. Video should now paint on its own WITHOUT touching cast. If it still needs cast, capture a fresh log — the `SURFACE[mid-playback+2s]` line plus whether the nudge ran narrows the next step (likely a forced controller re-attach).

## Release — EXECUTE EXACTLY (fix290 main-only; never checkout a tag)
```bash
cd /path/to/Free4Me-IPTV
git checkout main
git fetch origin
git reset --hard origin/main
git apply fix334.patch
flutter analyze --no-fatal-infos      # 2 tolerated INFOs
flutter test                          # 18/18 pass
grep '^version:' pubspec.yaml          # 1.26.50+334
grep -c "  '1.26.50':" lib/whats_new_modal.dart   # 1
grep -c '_FirstFrameNudge' lib/player/exo_engine.dart   # 3
git config core.hooksPath              # .githooks
git add -A
git commit -m "fix334: force ExoPlayer first-frame relayout so video paints without cast nudge (1.26.50)"
git push origin main
./scripts/build_and_release.sh
```
```bash
git fetch origin --tags
git merge-base --is-ancestor v1.26.50 origin/main && echo "v1.26.50 IS on main — GOOD" || echo "NOT on main — STOP"
git show origin/main:pubspec.yaml | grep '^version:'      # 1.26.50+334
git checkout v1.26.50 && python3 scripts/update_version_json.py
git diff --quiet version.json && echo "version.json CURRENT" || { echo "STALE"; git --no-pager diff version.json; }
git checkout main
```
