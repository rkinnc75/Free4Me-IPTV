# fix150.md — Cleanup

**Version:** Free4Me-IPTV 1.22.13 build.

Repository cleanup pass. No runtime code changes — touches docs, scripts, stray
files, and `.gitignore` only. The app and CI are unaffected.

---

## Scope

A repository hygiene pass covering documentation, the build/release script,
stray committed files, and ignore rules. The specifics are intentionally not
enumerated here; the executor performing this pass has the change list out of
band.

---

## Verify

After applying the cleanup:

```
flutter analyze --no-fatal-infos
```

Expected: only the two long-standing tolerated `use_build_context_synchronously`
INFOs in `settings_view.dart` (~1860 / ~2420). No new issues. (No Dart changed;
this is the standard pre-tag gate.)

---

## Ship

```
git add -A
git status   # review: confirm only intended files changed / removed
git commit -m "fix150: cleanup (1.22.13+150)"
git tag v1.22.13
git push "https://${PAT}@github.com/rkinnc75/Free4Me-IPTV.git" main
git push "https://${PAT}@github.com/rkinnc75/Free4Me-IPTV.git" refs/tags/v1.22.13
```

**Version:** `1.22.13+150` (even = phone). Tag `v1.22.13`.

---

## Notes

- Docs/scripts/ignore-rules only — no `lib/` changes; runtime and CI unaffected.
- Review `git status` before committing (this pass adds, edits, and removes files).
