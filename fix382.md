# fix382 — Cold-eyes bundle: in-memory search in a disabled category (HIGH-1), search-box stranding on cache-build failure (MED-1), stale root duplicates (LOW-1)

**Target version:** `1.34.11+382` (from `1.34.10+381`)
**Branch:** `main`
**Toolchain (verified in sandbox):** Flutter 3.44.0 / Dart 3.12.0 (CI pin), `libsqlite3.so` symlinked.
**Written against:** `v1.34.10+381` on main (commit `1cc2f33`). Findings were first identified on `v1.34.9+380` and re-confirmed unchanged on `v1.34.10+381` (fix381 — the Add Source single-screen change — did not touch any finding site).

## What & why

A cold-eyes review produced three findings; this runbook fixes all three.

**HIGH-1 — in-memory search returns 0 rows inside a disabled category.**
`VisibilityClause` (the SQL source of truth, fix371) appends the disabled-category
gate `cat_enabled = 1` **only when `groupId == null`** — so browsing *into* a
category shows its channels even if the category is disabled (fix302). The
in-memory `ChannelSearchCache.search` re-implements the same predicate in Dart,
but applied the disabled-category exclusion **unconditionally**:

    if (e.groupId != null && _disabledGroupIds.contains(e.groupId)) continue;

Reachable on defaults: `SearchMethod.inMemory` is the default search method,
entering a category sets `filters.groupId = node.id`, and disabled categories
stay listed/tappable (fix372 sorts them below; fix302 shows their channels). So:
open a disabled category → its channels are visible while scrolling, but the
moment the user types a query the list goes **empty**. Browsing shows them, and
the FTS/LIKE search methods show them — only the *default* in-memory search
over-filtered. Proven on a seeded in-memory sqlite DB (5 channels in disabled
category 10): the emitted `VisibilityClause` predicate for `(seriesId=null,
groupId=10)` returned **5 rows**; the unconditional cache form returned **0**.

Fix: add a `groupId == null` guard to the cache's disabled-category check so it
mirrors `VisibilityClause` byte-for-byte (the cache already receives `groupId`).

**MED-1 — search field can stay permanently disabled if the cache build throws.**
In `home.dart` `initializeAsync`, `_searchReady` is set `false` before the
in-memory cache warmup and re-enabled **only on the success path**, after an
un-guarded `await cacheFuture`. `ensureBuilt()` returns `rebuild().whenComplete(...)`
with no internal try/catch. If `rebuild()` throws (e.g. a transient DB read
error during warmup), the re-enable `setState` is skipped and the search
`TextField` (`enabled: _searchReady`) stays greyed out for the life of that
`Home` with no recovery.

Fix: re-enable `_searchReady` in a `finally`. `Sql.search` already tolerates an
unbuilt cache (it falls back to the SQL paths), so re-enabling is always safe.

**LOW-1 — stale, tracked, uncompiled duplicate files at the repo root.**
Seven tracked files sit at the repo root that Flutter never compiles (only
`lib/` is built) and that nothing imports: `db_factory.dart` (466 lines — stale
vs the live `lib/backend/db_factory.dart`, 713 lines), `sources_refresh_dialog.dart`
(stale vs `lib/widgets/sources_refresh_dialog.dart`), plus the stray artifacts
`pubspec.yaml.bak`, `BUILD-MACHINE-RULES (1).md`, `RUNBOOK-PREFLIGHT (1).md`,
`runbooks/fix172 (1).md`, and a file literally named `0`. No runtime effect; the
hazard is editor confusion (editing the root `db_factory.dart` silently does
nothing). Fix: `git rm` them (Release section).

## Files changed
- `lib/backend/channel_search_cache.dart` — HIGH-1: gate the disabled-category
  exclusion in `ChannelSearchCache.search` on `groupId == null` (braces added to
  satisfy `curly_braces_in_flow_control_structures` on the now multi-line `if`).
- `lib/home.dart` — MED-1: wrap the cache-warmup await in `_HomeState.initializeAsync`
  in try/catch/finally; re-enable `_searchReady` in the `finally`.
- `lib/whats_new_modal.dart` — `'1.34.11'` changelog entry (real UTF-8).
- `pubspec.yaml` — `1.34.10+381` → `1.34.11+382`.
- `test/in_memory_disabled_category_test.dart` — **new**: 2 tests. Runs the EXACT
  `VisibilityClause`-emitted predicate against a seeded sqlite DB and a Dart
  mirror of the cache filter, asserting equivalence for the browse-into-disabled-
  category case (SQL 5 == fixed-cache 5) and pinning the regression (old
  unconditional form returned 0). Same approach as `in_memory_sort_mode_test`.
- **Deletions (LOW-1, via `git rm` in Release):** `0`, `BUILD-MACHINE-RULES (1).md`,
  `RUNBOOK-PREFLIGHT (1).md`, `db_factory.dart`, `pubspec.yaml.bak`,
  `runbooks/fix172 (1).md`, `sources_refresh_dialog.dart`. Kept out of the patch
  on purpose: embedding ~1400 lines of dead file content would bury the 21-line
  code change. They are tracked, so `git rm` removes them mechanically.

## Evidence (run in sandbox on fresh main + patch + cleanup)
- `flutter analyze --no-fatal-infos` → **No issues found!**
- `flutter test` → **All tests passed! (64)** (62 baseline at +381, + 2 new).
- `git apply --check` of the embedded patch succeeds on a fresh clone of `main`;
  full chain (apply patch → `git rm` the 7 strays → analyze → test) is green on
  that clone.

## Pre-tag verification (build machine — exact checks)
1. `grep -c '^version: 1.34.11+382' pubspec.yaml` → **1**
2. `grep -n "'1.34.11':" lib/whats_new_modal.dart` → one hit
3. `grep -c 'groupId == null &&' lib/backend/channel_search_cache.dart` → **≥1** (the new HIGH-1 guard)
4. `grep -c '_disabledGroupIds.contains' lib/backend/channel_search_cache.dart` → **1** (single guarded site)
5. `grep -c 'ChannelSearchCache warmup failed' lib/home.dart` → **1** (the MED-1 catch branch)
6. `test -f test/in_memory_disabled_category_test.dart` → present
7. For each of `0`, `BUILD-MACHINE-RULES (1).md`, `RUNBOOK-PREFLIGHT (1).md`, `db_factory.dart`, `pubspec.yaml.bak`, `runbooks/fix172 (1).md`, `sources_refresh_dialog.dart`: `git ls-files --error-unmatch "<file>"` → **non-zero exit** (i.e. no longer tracked)
8. `flutter analyze --no-fatal-infos` → `No issues found!`
9. `flutter test` → `All tests passed!` (**64**)

## On-device integration check (HIGH-1 — the reachable scenario)
1. Install `v1.34.11+382` on any device with at least one source that has
   categories.
2. In the Categories view, disable a category that has channels (toggle it off).
   It stays in the list (sorted below the enabled ones).
3. Tap into that disabled category. Confirm its channels are listed (this already
   worked — browse uses the SQL path).
4. With the default search method (in-memory), type part of a channel name that
   exists in that category. **Pre-fix382:** the list went empty. **fix382:** the
   matching channels appear.
5. Back out to the top level (no category) and search the same term: a channel
   that lives only in the disabled category must NOT appear (disabled categories
   stay hidden from the flat/aggregated view — unchanged).

MED-1 is a fragile-state fix (no behaviour change unless the cache build throws);
no on-device step is required for it.

## Release — EXECUTE (main-only; CI owns version.json — do NOT run update_version_json.py before tagging; tags immutable)
git rm -q "0" "BUILD-MACHINE-RULES (1).md" "RUNBOOK-PREFLIGHT (1).md" "db_factory.dart" "pubspec.yaml.bak" "runbooks/fix172 (1).md" "sources_refresh_dialog.dart"
git add -A
git commit -m "fix382: in-memory search shows channels in a disabled category (cold-eyes HIGH-1); search box no longer stranded if the cache build fails (MED-1); remove stale tracked root duplicates (LOW-1); v1.34.11+382"

---

## Patch (verified: applies on fresh `main` @ 1cc2f33, analyze clean, 64/64)
```diff
diff --git a/lib/backend/channel_search_cache.dart b/lib/backend/channel_search_cache.dart
index a38acf4..931bc29 100644
--- a/lib/backend/channel_search_cache.dart
+++ b/lib/backend/channel_search_cache.dart
@@ -410,7 +410,18 @@ class ChannelSearchCache {
       // filter ran after the cache had already capped at `limit`, which let
       // dividers/disabled rows fill the page and hide real channels).
       if (e.isDivider && e.hideDividers) continue;
-      if (e.groupId != null && _disabledGroupIds.contains(e.groupId)) continue;
+      // fix382 (cold-eyes HIGH-1): mirror VisibilityClause — the disabled-
+      // category gate applies ONLY when NOT browsing INTO a specific category.
+      // Browsing into a disabled category (groupId set) must show its channels
+      // (fix302); the old unconditional check made in-memory search return 0
+      // results there, while the SQL FTS/LIKE/browse paths returned the full
+      // list (VisibilityClause appends `cat_enabled = 1` only when groupId is
+      // null). The `groupId == null` guard restores byte-equivalence.
+      if (groupId == null &&
+          e.groupId != null &&
+          _disabledGroupIds.contains(e.groupId)) {
+        continue;
+      }
       if (terms.isNotEmpty && !terms.every((t) => e.nameLower.contains(t))) {
         continue;
       }
diff --git a/lib/home.dart b/lib/home.dart
index c56d86e..188a17c 100644
--- a/lib/home.dart
+++ b/lib/home.dart
@@ -169,9 +169,19 @@ class _HomeState extends State<Home> {
 
     // Now wait for the cache to finish (no-op if not needed or already done).
     if (cacheFuture != null && mounted && !_searchReady) {
-      await cacheFuture;
-      AppLog.info('Home: ChannelSearchCache warmup completed');
-      if (mounted) setState(() => _searchReady = true);
+      // fix382 (cold-eyes MED-1): a cache-build failure must NOT strand the
+      // search field disabled. _searchReady was re-enabled only on the success
+      // path, so a throw from ensureBuilt()/rebuild() left the TextField greyed
+      // out for the life of this Home with no recovery. Re-enable in finally;
+      // Sql.search already tolerates an unbuilt cache (falls back to SQL).
+      try {
+        await cacheFuture;
+        AppLog.info('Home: ChannelSearchCache warmup completed');
+      } catch (e) {
+        AppLog.warn('Home: ChannelSearchCache warmup failed — $e');
+      } finally {
+        if (mounted) setState(() => _searchReady = true);
+      }
     }
 
     if (!mounted || !widget.refresh) return;
diff --git a/lib/whats_new_modal.dart b/lib/whats_new_modal.dart
index f860995..b2ad23b 100644
--- a/lib/whats_new_modal.dart
+++ b/lib/whats_new_modal.dart
@@ -6,6 +6,10 @@ import 'package:open_tv/backend/settings_service.dart';
 /// in that minor". The dialog shows all entries for [version] whose key is
 /// a prefix of the running version string.
 const _changelog = <String, List<String>>{
+  '1.34.11': [
+    'Fixed search returning nothing when you open a disabled category and start typing — the channels are right there when you scroll, and now they show up in search too. (Browsing and the other search modes were already correct; only the default in-memory search was over-filtering.)',
+    'A rare search-cache startup hiccup can no longer leave the search box permanently greyed out.',
+  ],
   '1.34.10': [
     'Add Source screen is now a single page: pick the provider type and every field for it (URL, username/password for Xtream, M3U file picker, optional EPG URL) appears at once. The old 4–5 step wizard is gone, replaced by one form with an "Add Source" button.',
   ],
diff --git a/pubspec.yaml b/pubspec.yaml
index e8f149f..10e739b 100644
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -16,7 +16,7 @@ publish_to: "none" # Remove this line if you wish to publish to pub.dev
 # https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
 # In Windows, build-name is used as the major, minor, and patch parts
 # of the product and file versions while build-number is used as the build suffix.
-version: 1.34.10+381
+version: 1.34.11+382
 
 environment:
   sdk: ^3.10.7
diff --git a/test/in_memory_disabled_category_test.dart b/test/in_memory_disabled_category_test.dart
new file mode 100644
index 0000000..577eee2
--- /dev/null
+++ b/test/in_memory_disabled_category_test.dart
@@ -0,0 +1,158 @@
+// fix382 (cold-eyes HIGH-1): the in-memory ChannelSearchCache applied the
+// disabled-category exclusion UNCONDITIONALLY, while VisibilityClause (the SQL
+// source of truth) applies it ONLY when groupId == null. So browsing INTO a
+// disabled category and searching returned 0 rows on the default in-memory
+// path, while the SQL FTS/LIKE/browse paths returned the category's channels
+// (fix302: browsing into a category shows its channels regardless of the
+// enabled checkbox).
+//
+// This test proves equivalence the rigorous way (same approach as
+// in_memory_sort_mode_test): it runs the EXACT `VisibilityClause`-emitted
+// predicate against a seeded sqlite DB for the (seriesId=null, groupId=<disabled
+// cat>) case, runs a Dart mirror of ChannelSearchCache's per-entry filter over
+// the SAME rows, and asserts the two id sets are identical. It also pins the
+// regression: the OLD unconditional form returned 0 for that case.
+import 'package:flutter_test/flutter_test.dart';
+import 'package:sqlite3/sqlite3.dart' as s3;
+import 'package:open_tv/backend/visibility_clause.dart';
+
+bool _sqliteAvailable() {
+  try {
+    s3.sqlite3.openInMemory().dispose();
+    return true;
+  } catch (_) {
+    return false;
+  }
+}
+
+/// One seeded channel. Mirrors the fields both the cache and the SQL filter on.
+class _Row {
+  final int id;
+  final int mediaType;
+  final int sourceId;
+  final int? seriesId;
+  final int? groupId;
+  final bool isDivider;
+  final bool catEnabled; // denormalized groups.enabled
+  const _Row(this.id, this.mediaType, this.sourceId, this.seriesId,
+      this.groupId, this.isDivider, this.catEnabled);
+}
+
+// Category 10 is DISABLED (catEnabled=false); category 20 is ENABLED.
+const _seed = <_Row>[
+  _Row(1, 2, 1, null, 10, false, false),
+  _Row(2, 2, 1, null, 10, false, false),
+  _Row(3, 2, 1, null, 10, false, false),
+  _Row(4, 2, 1, null, 10, false, false),
+  _Row(5, 2, 1, null, 10, false, false),
+  _Row(6, 2, 1, null, 20, false, true),
+  _Row(7, 2, 1, null, 20, false, true),
+  _Row(8, 2, 1, null, 20, false, true),
+];
+
+// --- Dart side: mirrors ChannelSearchCache.search's per-entry filter loop. ---
+// [disabledFixed] selects the fix382 behaviour (gate on groupId==null) vs the
+// pre-fix382 unconditional behaviour.
+List<int> _cacheFilter({
+  required int? groupId,
+  required Set<int> disabledGroupIds,
+  required bool disabledFixed,
+}) {
+  const mediaTypes = {2};
+  const sourceIds = {1};
+  const int? seriesId = null;
+  final out = <int>[];
+  for (final e in _seed) {
+    if (!mediaTypes.contains(e.mediaType)) continue;
+    if (!sourceIds.contains(e.sourceId)) continue;
+    if (seriesId != null && e.seriesId != seriesId) continue;
+    if (seriesId == null && e.seriesId != null) continue;
+    if (groupId != null && e.groupId != groupId) continue;
+    if (e.isDivider) continue; // hideDividers irrelevant here (none are dividers)
+    final hitDisabled = e.groupId != null && disabledGroupIds.contains(e.groupId);
+    if (disabledFixed) {
+      if (groupId == null && hitDisabled) continue; // fix382
+    } else {
+      if (hitDisabled) continue; // pre-fix382 (unconditional) — the bug
+    }
+    out.add(e.id);
+  }
+  return out;
+}
+
+s3.Database _seedDb() {
+  final db = s3.sqlite3.openInMemory();
+  db.execute('CREATE TABLE sources(id INTEGER PRIMARY KEY, hide_dividers INT DEFAULT 0)');
+  db.execute('''CREATE TABLE channels(
+    id INTEGER PRIMARY KEY, media_type INT, source_id INT, url TEXT,
+    series_id INT, group_id INT, is_divider INT DEFAULT 0, cat_enabled INT DEFAULT 1)''');
+  db.execute('INSERT INTO sources(id,hide_dividers) VALUES (1,0)');
+  for (final r in _seed) {
+    db.execute(
+      'INSERT INTO channels(id,media_type,source_id,url,series_id,group_id,is_divider,cat_enabled) '
+      'VALUES (?,?,?,?,?,?,?,?)',
+      [r.id, r.mediaType, r.sourceId, 'http://x', r.seriesId, r.groupId,
+       r.isDivider ? 1 : 0, r.catEnabled ? 1 : 0],
+    );
+  }
+  return db;
+}
+
+// Run the REAL VisibilityClause-emitted predicate appended to the browse base.
+List<int> _sqlIds(s3.Database db, {required int? seriesId, required int? groupId}) {
+  final (visSql, visParams) =
+      VisibilityClause.build(alias: 'c.', seriesId: seriesId, groupId: groupId);
+  final sql =
+      'SELECT c.id FROM channels c WHERE c.media_type = 2 AND c.source_id = 1 '
+      'AND c.url IS NOT NULL$visSql ORDER BY c.id';
+  return db.select(sql, visParams).map((r) => r['id'] as int).toList();
+}
+
+void main() {
+  group('in-memory cache vs VisibilityClause: disabled category (fix382)', () {
+    test('browsing INTO disabled category 10 — SQL shows its channels, '
+        'fixed cache matches, old cache returned 0', () {
+      if (!_sqliteAvailable()) {
+        markTestSkipped('sqlite native lib unavailable');
+        return;
+      }
+      final db = _seedDb();
+      try {
+        final sqlIds = _sqlIds(db, seriesId: null, groupId: 10);
+        final fixedIds =
+            _cacheFilter(groupId: 10, disabledGroupIds: {10}, disabledFixed: true);
+        final oldIds =
+            _cacheFilter(groupId: 10, disabledGroupIds: {10}, disabledFixed: false);
+
+        // SQL path returns all 5 channels of the disabled category (groupId set
+        // ⇒ VisibilityClause drops the cat_enabled gate).
+        expect(sqlIds, [1, 2, 3, 4, 5]);
+        // fix382 cache mirrors the SQL exactly.
+        expect(fixedIds, sqlIds);
+        // Regression guard: the pre-fix382 unconditional form returned nothing.
+        expect(oldIds, isEmpty);
+      } finally {
+        db.dispose();
+      }
+    });
+
+    test('top-level browse (groupId null) still hides disabled-category rows '
+        '— equivalence preserved', () {
+      if (!_sqliteAvailable()) {
+        markTestSkipped('sqlite native lib unavailable');
+        return;
+      }
+      final db = _seedDb();
+      try {
+        final sqlIds = _sqlIds(db, seriesId: null, groupId: null);
+        final fixedIds =
+            _cacheFilter(groupId: null, disabledGroupIds: {10}, disabledFixed: true);
+        // Only the enabled category's channels appear; disabled-cat rows hidden.
+        expect(sqlIds, [6, 7, 8]);
+        expect(fixedIds, sqlIds);
+      } finally {
+        db.dispose();
+      }
+    });
+  });
+}
```
